#include <stdio.h>
#include <stlib.h>

//allocates a specified number of bytes and returns a pointer to the first byte

int main(){
    int *ptr = (int*)malloc(5*sizeof(int)) //space for 5 integers
    return 0;
}