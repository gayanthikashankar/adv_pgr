#include <stdio.h>
#include <stlib.h>

//allocates a specified number of bytes and returns a pointer to the first byte
// difference from MALLOC: INITIALIZEDALLOCATED MEMORY TO ZERO

int main(){
    int *ptr = (int*)calloc(5, sizeof(int)) 
    return 0;
}